export interface HexConfig {
    apiKey: string;
    feedName: string;

    allowLinking?: boolean;
    bgColor?: string;
    hexSize?: string;
    primaryColor?: string;
    secondaryColor?: string;
    tertiaryColor?: string;
}
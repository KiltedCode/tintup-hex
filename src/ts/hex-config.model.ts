export interface HexConfig {
    apiKey: string;
    feedName: string;

    allowLinking?: boolean;
    bgColor?: string;
    primaryColor?: string;
    secondaryColor?: string;
    tertiaryColor?: string;
}